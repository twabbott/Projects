// Import only what we need from lib.js.
import { setMessage, getMessage } from './lib.js';

// Set the message
setMessage('Hello, world!');

// Get the message and print it.
console.log(getMessage());

console.log(window);


